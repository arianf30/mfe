name: deploy-container

on:
  push:
    branches:
      - main
    paths:
      - 'packages/container/**'

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://mfe-dashboard-arian/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIA5DGBSUTKD7GNPAPD
          AWS_SECRET_ACCES_KEY: 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
          AWS_DEFAULT_REGION: sa-east-1
